syntax="proto3";

package authentication;

option go_package="github.com/samarthasthan/e-commerce";

message Credentials {
    string email = 1;
    string password = 2;
}

service Authentication{
    rpc SignUp(Credentials) returns (Credentials);
    rpc Login(Credentials) returns (Credentials);
}